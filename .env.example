# Scam Sentinel — environment variable template
# Copy to `.env` and fill in real values. `.env` is gitignored.

# HuggingFace Hub access token (https://huggingface.co/settings/tokens)
# Required for finetune_gemma4_e2b.ipynb to push the LoRA adapter.
HF_TOKEN=hf_your_token_here
